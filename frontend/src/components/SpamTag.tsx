export function SpamTag() {
  return (
    <span
      className="inline-flex items-center px-2 py-0.5 rounded-md text-xs font-semibold font-display"
      style={{ background: 'var(--cs-bg-surface)', color: 'var(--cs-text-muted)', border: '1px solid var(--cs-border)' }}
    >
      SPAM?
    </span>
  );
}
